DATABASE_URL=postgresql://postgres:postgres@localhost:5432/marks_entry_db

# Run `python generate_admin_credentials.py` to generate real values for
# the three lines below - do not use these placeholders as-is.
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
JWT_SECRET=
ACCESS_TOKEN_EXPIRE_MINUTES=120
